<?xml version="1.0" encoding="utf-8"?>
<!-- initialLayout for the prayer widget: what the launcher shows between the widget
     being dropped and Glance delivering its first RemoteViews. Kept to a single
     TextView so it costs nothing and never flashes a differently-shaped card. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/prayer_widget_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/prayer_widget_loading"
        android:textColor="@color/prayer_widget_muted"
        android:textSize="13sp" />
</LinearLayout>
