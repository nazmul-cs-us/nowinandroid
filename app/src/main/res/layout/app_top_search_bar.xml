<?xml version="1.0" encoding="utf-8"?>
<!-- Ported from material-components-android catalog cat_search_appbar_icons_fragment.xml.
     Scaffold layout: the CoordinatorLayout is full-screen so the SearchView has
     room to expand to fullscreen via setupWithSearchBar(). App content is hosted
     inside content_container via a ComposeView added programmatically. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.Material3Expressive.AppBarWithSearch"
        android:fitsSystemWindows="false">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/leading_button"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="4dp"
                android:contentDescription="@string/app_top_bar_leading_icon_description"
                style="?attr/materialIconButtonStyle"
                app:icon="@drawable/ic_app_top_bar_profile_24"
                app:iconSize="34dp"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                android:insetLeft="0dp"
                android:insetRight="0dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:paddingStart="0dp"
                android:paddingEnd="0dp"
                android:paddingLeft="0dp"
                android:paddingRight="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Start margin (12dp) is larger than end (8dp) to offset the 34dp
                 leading avatar against the 26dp settings glyph, keeping the visible
                 gaps on both sides of the pill symmetric without shifting on login. -->
            <com.google.android.material.search.SearchBar
                android:id="@+id/app_search_bar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginVertical="4dp"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="8dp"
                android:layout_gravity="center_vertical"
                android:hint="@string/app_top_bar_search_hint"
                app:defaultMarginsEnabled="false"
                app:hideNavigationIcon="false"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toRightOf="@id/leading_button"
                app:layout_constraintRight_toLeftOf="@id/settings_button"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintWidth_max="312dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/settings_button"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="4dp"
                android:scaleType="center"
                android:layout_gravity="center_vertical"
                android:contentDescription="@string/app_top_bar_settings_icon_description"
                style="?attr/materialIconButtonStyle"
                app:icon="@drawable/ic_app_top_bar_settings_24"
                app:iconSize="26dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:id="@+id/content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.search.SearchView
        android:id="@+id/app_search_view"
        style="@style/Widget.Material3Expressive.SearchView.AppBarWithSearch"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="@string/app_top_bar_search_hint"
        app:layout_anchor="@id/app_search_bar">

        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/search_suggestion_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </androidx.core.widget.NestedScrollView>
    </com.google.android.material.search.SearchView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
